{
  "name": "open-cat-cafe",
  "version": "0.1.0",
  "description": "Multi-agent collaboration system based on OpenCode CLI",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "check": "tsc --noEmit",
    "test": "node tests/integration.test.js",
    "test:modules": "node test-modules.js",
    "clean": "rimraf dist"
  },
  "keywords": [
    "agent",
    "mcp",
    "opencode",
    "multi-agent"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}